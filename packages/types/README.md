# @workforce-erp/types

Shared TypeScript types and interfaces used across applications and packages.

## Current surface

The package currently exports a generic `ApiResponse<TData>` interface.

## Intended use

Keep shared contracts here when multiple projects need the same types and you want one source of truth.
